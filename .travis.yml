language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-una-gen' ]; then cd .. && eval "mv $currentfolder generator-una-gen" && cd generator-una-gen; fi

